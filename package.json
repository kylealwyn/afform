{
  "name": "afform",
  "version": "0.1.0",
  "description": "Automatically validate your forms using the HTML5 builtin methods",
  "main": "afform.js",
  "scripts": {
    "start": "nodemon -i dist --exec npm run bundle",
    "bundle": "babel src/afform.js --out-file=dist/afform.js",
    "build": "node Makefile",
    "publish": "npm run build && np"
  },
  "keywords": [],
  "author": "Kyle Alwyn <http://kylealwyn.com>",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "np": "^2.12.0",
    "uglifyjs": "^2.4.10"
  }
}
